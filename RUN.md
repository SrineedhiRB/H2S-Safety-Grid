# Run

1. Extract the project.
2. Open a terminal in the project root.
3. Run:
   `python -m http.server 8000`
4. Open:
   `http://localhost:8000/frontend/index.html`

Use localhost rather than opening the HTML file directly because camera permissions and browser APIs work more reliably from an HTTP origin.
